# slice_library

"body" : {
      "type" : "Slices",
      "fieldset" : "Slice zone",
      "config" : {
        "choices" : {
        
          //All the JSON snippets in that repo to be inserted in any custom type JSON here
          
            }
          }
        }
